service: social-care-case-viewer-api
provider:
  name: aws
  vpc: ${self:custom.vpc.${opt:stage}}
  stage: ${opt:stage}
  region: eu-west-2

resources:
  Resources:
    TestBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: social-care-case-viewer-api-test

custom:
  vpc:
    production:
      securityGroupIds:
        - sg-048f37056608033d3
      subnetIds:
        - subnet-0665104ee973a21be
        - subnet-005b74d8082f68a84
